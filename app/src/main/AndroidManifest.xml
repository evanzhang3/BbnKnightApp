<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bbn.bbnknight">

    <application
        android:name=".BbnNotificationChannel"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".NotificaitonConfigureActivity" />
        <activity android:name=".ConfigureSpecialBlockActivity" />
        <activity android:name=".configureLunchBlockActivity" />
        <activity android:name=".addOrDelClassActivity" />
        <activity android:name=".FutureDayActivity" />
        <activity android:name=".SetClassActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>